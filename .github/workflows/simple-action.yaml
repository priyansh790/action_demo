name: simple js action
on: [push]

jobs:
  simple-action:
    runs-on: ubuntu-latest
    steps:
      - name: simple js action
        uses: actions/checkout@v4
      - name: second step
        run: |
          pwd
          ls
